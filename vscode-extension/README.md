# BSC Syntax Highlighting for Visual Studio Code
Syntax Highlighting and snippets for BSC files in Visual Studio Code.

## Installation

```bash
cd vscode-extension
vsce package --allow-missing-repository
code --install-extension bcs-x.x.x.vsix
```